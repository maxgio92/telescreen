[Unit]
Description=thinkpol, the acting layer (execute publish approvals deterministically)

[Service]
Type=oneshot
TimeoutStartSec=300
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
EnvironmentFile=-%h/.config/thinkpol.env
ExecStart={{exec}} thinkpol
